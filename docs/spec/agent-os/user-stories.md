# Agent OS ユーザーストーリー (逆生成)

**分析日時**: 2026-05-22

---

## ペルソナ

| ID | 名前 | 説明 |
|----|------|------|
| P1 | **開発者 (Daisuke)** | AI エージェントを日常の開発補助・情報整理に使う個人開発者 |
| P2 | **エージェント (Hermes)** | 自律的に動作し、知識を蓄積・検索するAIエージェント |

---

## ストーリー一覧

### US-001: ワンコマンド起動

- **である** 開発者 **として**
- **私は** `just up` の一回のコマンドで全サービスを起動 **したい**
- **そうすることで** 複雑な手順を覚えずに作業を開始できる

**実装根拠**: `justfile:6-19` — submodule 更新 → compose up → health → バナー表示

---

### US-002: チャット対話

- **である** 開発者 **として**
- **私は** ブラウザ (`localhost:8787`) で Hermes と自然言語でチャット **したい**
- **そうすることで** コマンドラインを使わずにエージェントに指示できる

**実装根拠**: `docker-compose.yml:130-152` — hermes-webui (SSE ストリーミング、tool-call カード)

---

### US-003: 知識の蓄積と検索

- **である** エージェント **として**
- **私は** GBRAIN に人物・企業・概念のページを書き込み、後で検索 **したい**
- **そうすることで** 会話を超えた長期知識を保持・活用できる

**実装根拠**: `config/hermes/setup-mcp.sh:47-55` — MCP tools: put_page, get_page, search, query

---

### US-004: ユーザー理解の自動深化

- **である** 開発者 **として**
- **私は** 会話するだけで Honcho がユーザーの好み・性格を自動学習 **してほしい**
- **そうすることで** 明示的に教えなくても応答品質が向上する

**実装根拠**: `docker-compose.yml:107-128` — honcho-deriver バックグラウンドワーカー

---

### US-005: 全体状況の一覧確認

- **である** 開発者 **として**
- **私は** 1 つのダッシュボード (`localhost:8080`) で全サービスの健康状態とメモリ成長を確認 **したい**
- **そうすることで** 問題の早期発見とシステムの成長を観測できる

**実装根拠**: `monitoring/public/index.html` — Health Grid + Memory Growth + Token Usage パネル

---

### US-006: 安全なリセット

- **である** 開発者 **として**
- **私は** `just clean` で全データを消去し、`just up` でゼロから再構築 **したい**
- **そうすることで** 壊れた状態からいつでも復帰でき、実験を恐れずに試せる

**実装根拠**: `justfile:40-45` — 確認プロンプト付きクリーンアップ

---

### US-007: ホスト非汚染

- **である** 開発者 **として**
- **私は** Mac / Linux ホストに Ollama / Docker / just 以外の依存を入れたくない
- **そうすることで** Agent OS をアンインストールすればホストが完全にクリーンに戻る

**実装根拠**:
- `config/hermes/config.yaml:19` — `terminal.backend: "local"` (コンテナ内実行)
- `README.md` — 痕跡消去セクション

---

### US-008: モデル切替

- **である** 開発者 **として**
- **私は** `.env` の `OLLAMA_MODEL` を変えるだけで LLM モデルを切り替え **たい**
- **そうすることで** 新モデルの評価やコスト最適化が簡単にできる

**実装根拠**:
- `docker-compose.yml:37` — `HERMES_MODEL: ${OLLAMA_MODEL:-...}`
- `config/hermes/setup-mcp.sh:29-37` — 起動時にモデル設定を注入

---

### US-009: メトリクス履歴の保持

- **である** 開発者 **として**
- **私は** GBRAIN/Honcho のメトリクスが初日からスナップショット保存 **されてほしい**
- **そうすることで** 将来グラフで成長を振り返れる

**実装根拠**:
- `monitoring/src/snapshots.ts` — 5 分間隔で `dashboard_metrics` テーブルに INSERT
- `config/postgres/init.sql:8-14` — テーブル + インデックス定義

---

### US-010: クロスプラットフォーム運用

- **である** 開発者 **として**
- **私は** Mac でも Linux でも同じ手順で Agent OS を動かし **たい**
- **そうすることで** 開発マシンを選ばずに作業できる

**実装根拠**:
- `docker-compose.yml` — `extra_hosts`, `OLLAMA_BASE_URL` 環境変数
- `README.md` — Mac / Linux セットアップ手順の両方を記載

---

### US-011: サブモジュールの更新

- **である** 開発者 **として**
- **私は** `just update` で vendor の最新版を取得して再ビルド **したい**
- **そうすることで** upstream の改善を簡単に取り込める

**実装根拠**: `justfile:69-77` — fetch tags → checkout → rebuild

---

### US-012: 個別サービスのデバッグ

- **である** 開発者 **として**
- **私は** `just logs hermes` でサービスごとのログを確認 **したい**
- **そうすることで** 問題の原因を素早く特定できる

**実装根拠**: `justfile:48-49` — `docker compose logs {{ args }}`
